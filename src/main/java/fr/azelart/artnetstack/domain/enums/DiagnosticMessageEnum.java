/**
 * 
 */
package fr.azelart.artnetstack.domain.enums;

/**
 * Diagnostic messages enumeration
 * @author Corentin Azelart.
 *
 */
public enum DiagnosticMessageEnum {

	BROADCAST,	// 0 = Diagnostics messages are broadcast. (if bit 2)
	UNICAST		// 1 = Diagnostics messages are unicast. (if bit 2).
	
}
