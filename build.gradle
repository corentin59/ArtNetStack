apply plugin: 'com.android.library'

android {
    compileSdkVersion 24
    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 24
        versionCode 18
        versionName '2.0.1'
    }
}

dependencies {
    testImplementation 'junit:junit:4.12'
}
